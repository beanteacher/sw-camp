 /* 함수는 매개변수의 개수와 인수의 개수가 일치하는지 체크하지 않는다.
 * 단, 인수가 부족해서 할당돠지 않은 매개변수 값은 undefined가 된다 */
 function hellO() {

 }
 var result = helloo();
 result = helloo("유관순", "홍길동");

 /* 인수를 전달하지 않거나 undefined를 전달하면 기본 값이 동작하다록 설ㅈ어 */
 function hi(name = '아무개') {
  return `R{}';`
 }